import { TileMap } from "./collision.js";

export const TILE_SIZE = 36;

/** @typedef {{ kind: "health" | "coin" }} PickupDef */
/** @typedef {{ variant?: "grunt" | "brute" | "gunner" }} BotDef */

/**
 * @typedef {{
 *   id: string;
 *   name: string;
 *   rows: string[];
 *   portal: { tw: number; th: number };
 *   pickups: PickupDef[];
 *   bots: BotDef[];
 * }} LevelDef
 */

/** # = wall, . = floor — layout only; player, portal, enemies, and pickup positions are randomized at load from valid floor / dead ends. */
export const LEVELS = /** @type {LevelDef[]} */ ([
  {
    id: "cellar",
    name: "Cellar",
    rows: [
      "##################################################",
      "#................................................#",
      "#..####..............................####........#",
      "#..#..#..............................#..#........#",
      "#..####....################..........####........#",
      "#...........#..............#.....................#",
      "#...........#..............#....####.............#",
      "#...........################....#..#.............#",
      "#...............................####.............#",
      "#................................................#",
      "#................................................#",
      "#................................................#",
      "#................................................#",
      "##################################################",
    ],
    portal: { tw: 2, th: 4 },
    pickups: [{ kind: "health" }, { kind: "coin" }, { kind: "coin" }],
    bots: [{ variant: "grunt" }, { variant: "gunner" }],
  },
  {
    id: "catacombs",
    name: "Catacombs",
    rows: [
      "##################################################",
      "#....................##..........................#",
      "#..########...........##...........########......#",
      "#..#......#...........##...........#......#......#",
      "#..#......#...........##...........#......#......#",
      "#..########...........##...........########......#",
      "#................................................#",
      "#..################............################..#",
      "#..#..............#............#..............#..#",
      "#..#..............#............#..............#..#",
      "#..################............################..#",
      "#................................................#",
      "#................................................#",
      "##################################################",
    ],
    portal: { tw: 2, th: 3 },
    pickups: [{ kind: "coin" }, { kind: "health" }, { kind: "coin" }],
    bots: [{ variant: "grunt" }, { variant: "brute" }, { variant: "gunner" }],
  },
  {
    id: "depths",
    name: "Depths",
    rows: [
      "##################################################",
      "#..........####################..................#",
      "#..........#.................#...................#",
      "#..........#.................#...................#",
      "#............................#...................#",
      "#..........#.................#...................#",
      "#..........#.................#...................#",
      "#..........####################..................#",
      "#................................................#",
      "#..##########################################....#",
      "#..#........................................#....#",
      "#..#........................................#....#",
      "#..##########################################....#",
      "##################################################",
    ],
    portal: { tw: 2, th: 3 },
    pickups: [{ kind: "coin" }, { kind: "coin" }, { kind: "health" }],
    bots: [{ variant: "grunt" }, { variant: "gunner" }, { variant: "brute" }],
  },
]);

/**
 * @param {string[]} rowStrings
 */
export function parseTilesFromRows(rowStrings) {
  return rowStrings.map((row) =>
    row.split("").map((ch) => (ch === "#" ? 1 : 0)),
  );
}

/**
 * @param {number} tx
 * @param {number} ty
 */
export function tileCenter(tx, ty) {
  return {
    x: (tx + 0.5) * TILE_SIZE,
    y: (ty + 0.5) * TILE_SIZE,
  };
}

/**
 * @param {LevelDef} def
 */
export function buildTileMap(def) {
  const tiles = parseTilesFromRows(def.rows);
  return new TileMap(TILE_SIZE, tiles);
}

/**
 * Portal AABB in world pixels.
 * @param {{ tx: number; ty: number; tw: number; th: number }} p
 */
export function portalWorldRect(p) {
  return {
    x: p.tx * TILE_SIZE,
    y: p.ty * TILE_SIZE,
    w: p.tw * TILE_SIZE,
    h: p.th * TILE_SIZE,
  };
}
